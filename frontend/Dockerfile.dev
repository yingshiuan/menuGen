FROM node:20-alpine
WORKDIR /app

# Install deps (include dev)
COPY package*.json ./
RUN npm ci

# Copy source
COPY . .

ENV HOST=0.0.0.0
ENV PORT=5173
EXPOSE 5173
# Ensure Vite binds to 0.0.0.0 inside the container
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
